# environment variables

export DATABASE_URL=postgresql://postgres:password@localhost:5432/oquest
export APP_NAME=oquest-backend
export APP_HOST=127.0.0.1
export APP_PORT=3000
cargo run -p backend

### Setup
PostgreSQL (yes ofc)
make a cute little .env